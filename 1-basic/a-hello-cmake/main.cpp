#include <iostream>
#include <sstream>
#include <string>
using namespace std;

int main(int argc, char *argv[])
{
   unsigned char a = 0;
   string name ="lkz";
      cout << name.size()  << endl;

   for (uint8_t i = 0; i < 0xff; i++)
   {
      // stringstream ss;
      // ss <<  "|" << to_string(i) << "|";
   }
   return 0;
}